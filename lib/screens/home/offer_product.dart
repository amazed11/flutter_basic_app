import 'package:flutter/material.dart';

class OfferProduct extends StatelessWidget {
  const OfferProduct({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Offer Product"),
      ),
    );
  }
}
